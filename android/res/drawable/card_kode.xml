<?xml version="1.0" encoding="utf-8"?>
<!-- Kartu blok kode di gelembung AVI: lubang paling gelap + garis tipis. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="14dp"/>
    <solid android:color="@color/dasar_kode"/>
    <stroke android:width="1dp" android:color="@color/garis"/>
</shape>
