<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.avi.assistant"
    android:versionCode="2"
    android:versionName="1.1">

    <uses-sdk android:minSdkVersion="24" android:targetSdkVersion="34"/>

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="com.android.alarm.permission.SET_ALARM"/>

    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN"/>
            <category android:name="android.intent.category.LAUNCHER"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
    </queries>

    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/TemaAVI"
        android:allowBackup="true"
        android:supportsRtl="true">

        <activity android:name=".MainActivity" android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- LAYAR ORBIT = wajah asisten perangkat versi ACTIVITY.
             Hasil penelusuran AOSP 12: AssistManager.startAssist()
             meluncurkan ACTIVITY ber-intent ACTION_ASSIST bila isi
             Settings.Secure.ASSISTANT bukan VoiceInteractionService
             aktif; dan pada perangkat low-RAM (umum di itel/Transsion)
             jalur VoiceInteractionService bahkan dilewati saat
             kualifikasi (AssistantRoleBehavior) — asisten SELALU
             dijalankan sebagai activity, persis model "Google Assistant
             Go". Activity ini translucent, tanpa jejak Recents, dan
             menutup sendiri saat AVI pamit. Perangkat non-low-RAM tetap
             memakai jalur sesi overlay (AviSession) — tampilan identik. -->
        <activity android:name=".OrbitAssistActivity"
            android:exported="true"
            android:theme="@style/TemaSesiOrbit"
            android:label="@string/app_name"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:taskAffinity="">
            <!-- Jalur kualifikasi asisten ALTERNATIF: perangkat yang
                 ditandai low-RAM MELEWATI pengecekan
                 VoiceInteractionService dan hanya menerima asisten
                 ber-intent ASSIST. Inilah pola yang dipakai Google
                 Assistant & Dicio. -->
            <intent-filter>
                <action android:name="android.intent.action.ASSIST"/>
                <action android:name="android.intent.action.VOICE_COMMAND"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
            <meta-data android:name="com.android.systemui.action_assist_icon"
                android:resource="@mipmap/ic_launcher"/>
        </activity>

        <activity android:name=".SettingsActivity" android:exported="false"/>

        <!-- Kalibrasi suara pemilik: ukur kenyaringan + uji perintah nyata
             dengan suara pemilik sendiri (hasil disimpan lokal). -->
        <activity android:name=".KalibrasiActivity" android:exported="false"/>
        <activity android:name=".LiveActivity" android:exported="false"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustNothing"/>
        <activity android:name=".OnboardingActivity" android:exported="false"/>

        <!-- Asisten perangkat: AVI dipanggil lewat tahan tombol home /
             sapu sudut bawah, seperti Google Assistant. Pemicu dipegang
             sistem — tidak ada layanan latar, baterai tetap aman. -->
        <service android:name=".AviVoiceInteractionService"
            android:exported="true"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_VOICE_INTERACTION">
            <intent-filter>
                <action android:name="android.service.voice.VoiceInteractionService"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
            <meta-data android:name="android.service.voice"
                android:resource="@xml/avi_assisten"/>
        </service>
        <service android:name=".AviSessionService"
            android:exported="true"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_VOICE_INTERACTION"/>

        <!-- Pengenal suara AVI: WAJIB didaftarkan di meta-data asisten
             (recognitionService) — tanpa itu AVI tidak pernah muncul di
             daftar asisten digital (Android 10+ menolak parse). Sesinya
             diproksikan ke pengenal bawaan perangkat (mis. Google). -->
        <service android:name=".AviRecognitionService"
            android:exported="false"
            android:label="@string/app_name"/>
    </application>
</manifest>
